/*
	Cyclone Script demo
*/
host PrintString (P);
host kk(P);
#include  "define.c2ds"
func test(time)
{
PrintString("--->"$time);
}
func _Main ()
{
var v;
var str;
var Str;
str=CONST_pound$CONST_star$"++";
v=0;
for(;v<6;v++)
{
	var v2;
	for(v2=0;v2<5;v2++)
	{
		test(str);
                        kk(2);
		str$="*";
		if(v2>2)
		{
		break;
		}
	}
}
} 